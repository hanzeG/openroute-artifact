# Reproduction Commands

Use the command sequence in `README.md`.

The full run writes generated XRPL metadata, prebook snapshots, fit outputs, and
optimisation outputs under `artifacts/`, which is ignored by Git.
