This directory contains lightweight Delta-derived ranking tables used to select
the ten paper token pairs.

The main file used to recover the paper pair identifiers is:

```text
target_window_rankings/top15_xrp_pairs_combined_clean_20260512.csv
```

The AMM/CLOB/fee parquet rows used by the two-week experiments are bundled
separately under `data/delta_exports/`.
